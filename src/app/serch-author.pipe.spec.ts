import { SerchAuthorPipe } from './serch-author.pipe';

describe('SerchAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new SerchAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
