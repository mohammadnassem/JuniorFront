import { SerchReservedPipe } from './serch-reserved.pipe';

describe('SerchReservedPipe', () => {
  it('create an instance', () => {
    const pipe = new SerchReservedPipe();
    expect(pipe).toBeTruthy();
  });
});
